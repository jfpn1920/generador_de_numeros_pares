## 👋 ¡Bienvenidos usuarios a mi proyecto! generador de numeros pares

<img src="imagen_presentacion.png" alt="Presentación" width="205" align="left" style="margin-right:20px; border-radius:5px;">  
<p style="text-align: justify;">

Este proyecto consiste en un **generador de números pares**, el cual muestra todos los números pares dentro de un rango definido por el usuario. El programa solicita un número inicial y un número final, y posteriormente recorre ese rango para identificar cuáles valores cumplen la condición de ser pares.

El sistema utiliza un **bucle `for`** para recorrer la secuencia numérica y **condicionales** junto con **operadores matemáticos** para determinar si un número es divisible entre 2. Los resultados se muestran en pantalla de forma clara y ordenada.

El objetivo principal del proyecto es reforzar el uso de **estructuras repetitivas**, **condicionales** y **operadores aritméticos**, además de mejorar la lógica para el manejo de rangos numéricos y la validación de condiciones en Python.

#
### 🧑‍💻 Lenguaje de programacion
- Python

#
### 🎯 Objetivos del proyecto
- Comprender el uso del bucle `for` para recorrer rangos numéricos.
- Aplicar condicionales para filtrar datos según una condición específica.
- Identificar números pares mediante operaciones matemáticas.
- Fortalecer la lógica de programación básica en Python.

#
### 🧠 Temas que se a aplicado
- Variables
- Entrada de datos (`input`)
- Bucles `for`
- Condicionales `if`
- Operadores matemáticos (`%`)
- Salida de datos (`print`)

#
### ⚙️ Funcionamiento
1. El programa solicita al usuario un número inicial y un número final.
2. Se recorre el rango de valores usando un bucle `for`.
3. En cada iteración, se verifica si el número es par.
4. Si cumple la condición, el número se muestra en pantalla.
5. Al finalizar, el usuario obtiene la lista completa de números pares del rango ingresado.

#
### ▶️ Cómo usar el proyecto
Tienes dos opciones para obtener el código:
1. **Descargar directamente:**
   Haz clic en el botón verde code y selecciona download zip.

2. **Clonar con git:**
   ```bash
   git clone https://github.com/tu-usuario/tu-repositorio.git
   ```